import React from 'react'

export default function StepUserName({onNext}) {
    return (
        <>
        <div>
            StepUserName
        </div>
        <button onClick={onNext}>Next</button>
        </>
    )
}
